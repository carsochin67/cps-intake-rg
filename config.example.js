// TEMPLATE — safe to commit. Copy this file to config.js and fill in the real
// values. config.js is gitignored so the secret never lands in git/GitHub.
//
//   cp config.example.js config.js   (then edit config.js)
//
window.CPS_CONFIG = {
  API_BASE: 'https://YOUR-FUNCTION-APP.azurewebsites.net',
  API_CODE: 'PASTE_FUNCTION_KEY_HERE'
};
